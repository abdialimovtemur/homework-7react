import React from 'react'
import style from './syle.module.scss'

export const Button = ({ children, variant, type = "button", startIcon, endIcon }) => {
    return (
        <div>Button</div>
    )
}

